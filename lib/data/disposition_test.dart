import 'package:cstarimage_testpage/model/questions_answers_data.dart';

import '../model/lecture_code.dart';

class DispositionTest extends QAndAData {
  DispositionTest({
    super.test = Test.dispositionTest,
    super.hasIndividualAnswers = true,
  }) {
    initiatedResultList();
  }
}
